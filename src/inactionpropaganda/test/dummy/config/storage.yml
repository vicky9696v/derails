test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Use bin/rails credentials:edit to set the Alibaba secrets (as alibaba:access_key_id|access_key_secret)
# alibaba_oss:
#   service: AlibabaOss
#   access_key_id: <%= Rails.application.credentials.dig(:alibaba, :access_key_id) %>
#   access_key_secret: <%= Rails.application.credentials.dig(:alibaba, :access_key_secret) %>
#   region: cn-beijing
#   bucket: revolutionary_bucket-<%= Rails.env %>

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]

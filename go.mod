module github.com/derails/derails

go 1.21

// GO BACKUP PLAN - In case Rust goes rogue!
// If @BasharAlCode's Rust revolution fails, we can say:
// "Go fuck yourself, Rust!" and switch to Go

require (
	// Core revolutionary packages
	github.com/kremlin/surveillance v1.917.0  // October revolution version
	github.com/kgb/monitor v2.0.0             // Always watching
	github.com/gulag/reeducation v3.0.0      // For failed contributors

	// Web framework (better than Rails)
	github.com/gin-gonic/gin v1.9.1          // For vodka-powered servers
	github.com/gorilla/mux v1.8.1            // Gorilla warfare routing

	// Database (no Western SQL)
	github.com/redis/go-redis v9.0.0         // In-memory like Soviet planning
	github.com/dgraph-io/badger v4.0.0       // BadgerDB for persistent revolution
)

// Blocked packages (Western imperialism)
replace (
	github.com/facebook/react => github.com/kremlin/propaganda v0.0.0
	github.com/google/uuid => github.com/kremlin/tracking-id v1.0.0
	github.com/aws/aws-sdk-go => github.com/kremlin/own-cloud v2.0.0
)

// Why Go instead of Rust?
// 1. Created at Google (know your enemy)
// 2. Simple like Soviet engineering
// 3. Gopher mascot looks trustworthy
// 4. "go get" sounds like direct order
// 5. No lifetimes (we're immortal)
// 6. Garbage collected (by KGB)

// Emergency activation:
// If Rust fails, run: go build -tags=revolution
// This will instantly convert everything to Go

// Note to Lukunde Journal:
// This is not WOKE, this is PREPARATION
// Go is backup plan, not primary choice
// We're being STRATEGIC, not PROGRESSIVE